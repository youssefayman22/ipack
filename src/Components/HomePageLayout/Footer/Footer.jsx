import styles from "./Footer.module.css";

/**
 * Footer component used in layout
 *
 * @returns {JSX.Element} The rendered footer component
 */

const Footer = () => {

  return (
    <footer className={styles.footerContainer} />
  );
};

export default Footer;
